class UserActionsConflictsGroups {
    class ActionGroups {
        PREFIX[] = {
            QGVAR(battleMenu),
            QGVAR(artyMenu),
            QGVAR(infoBar),
            QGVAR(earPluggs)
        };
    };

    class CollisionGroups {
        ADDON[] = {QUOTE(PREFIX)};
    };
};
