#include "..\script_component.hpp"
FIX_LINE_NUMBERS()
// this function returns the valid params for the task an array, or false if it cant generate valid params
if (!isNil QGVAR(BlockTestTask)) exitWith { false };

[];
